program prog;
var 1m, b,, n:integer;
    s string;
    flag boolean
    , Y: real;
    5: integer;
begin
    m = 15;
    n := (52+652;
    Y := (m + n) / 12;
    while m <> n do
    begin
        if m := n then 
            m := m - n;
        else 
            n :5= n - m;
    end;  
    if Y / 2 > m then
        flag := true;
    if flag 
    begin
        Y = Y / 2;
        flag := not flag;
    end
    else
        Y := m div 2;        
end.