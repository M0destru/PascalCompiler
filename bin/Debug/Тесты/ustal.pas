program ustal; // Привет :), а что ты тут делаешь? Этот файл тебе не нужен, закрывай
var r: real;
  a: integer;
begin
  writeln (4 / 2);
  writeln (4 / 3);
  writeln (2 / 4);
  writeln (5 / 1.2);
  writeln (5 / 1.8);
  writeln (1.8 / 5);
  writeln (1.8 / 0.9);
  writeln (0.9 / 1.8);
  writeln (1.8 / 1.6);
  writeln ('pes' + 'bikoz');
  writeln ('pes' + 'pes' + 'pes' + 'pes');
  a := 5;
  r := a + 2.5;
  writeln(r);
  r := a;
  writeln(r);
  r := 5-3;
  writeln(r);
  r := 5.5 / 2.5;
  writeln(r);
end.